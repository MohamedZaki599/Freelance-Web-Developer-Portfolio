# Portfolio Law Firm WordPress Theme

Run this from the project folder:

```bash
npm run theme:build
```

Then upload or zip the `wordpress-theme` folder as the WordPress theme. The generated Next.js static files will be placed in `wordpress-theme/static`.
